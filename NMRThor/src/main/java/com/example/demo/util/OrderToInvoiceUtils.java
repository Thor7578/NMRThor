package com.example.demo.util;

public class OrderToInvoiceUtils {

    private OrderToInvoiceUtils(){}

    private static void progressOrder(int orderID){
        //Add a new invoice with the exact same information plus extra (like the Invoice-class)
        //Delete order with above ID from SQL orders list
    }
}
